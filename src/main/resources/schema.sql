CREATE TABLE hero (
                      id BIGINT AUTO_INCREMENT PRIMARY KEY,
                      superhero VARCHAR(50),
                      publisher VARCHAR(50),
                      alter_ego VARCHAR(50)
);
